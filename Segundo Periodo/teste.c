
int main(void)
{
    char entrada;
    int numMov, movimento, i;
    int vetor[] = {0, 0, 0};
    int vetorsup[] = {0};

    scanf("%d %c", &numMov, &entrada);

    printf("%d",vetor);
    return 0;
}
